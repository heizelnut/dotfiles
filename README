dotfiles [Wayland]
==================
to use them, see the Makefile

what's on there?
 + Hyprland config and startup scripts (waysetup.sh)
 + .zshrc config with some environment variables (.zshenv) and PS1 prompt
 + scripts with eventually some assets (.config/assets)
 + .config files for all the other stuff
 + various cronjobs
